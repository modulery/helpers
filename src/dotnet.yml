name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: netstandard2.1
    - name: Restore dependencies
      run: dotnet restore Moduler.Helpers.csproj
      working-directory: src/Moduler.Helpers
    - name: Build
      run: dotnet build Moduler.Helpers.csproj --configuration Release --no-restore
      working-directory: src/Moduler.Helpers
    - name: Pack
      run: dotnet pack Moduler.Helpers.csproj --configuration Release --no-build --output ./bin/Release
      working-directory: src/Moduler.Helpers
    - name: NuGet Push
      run: dotnet nuget push "./bin/Release/*.nupkg" --source "github" --skip-duplicate
      working-directory: src/Moduler.Helpers
